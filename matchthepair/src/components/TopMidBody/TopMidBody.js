import React from "react";
import "./TopMidBody.css";

const TopMidBody = props => (
    <div>
        <h1>Clicky Game!</h1>
        <h2>Click on an image to earn points, but don't click on any more than once!</h2>
    </div>
);

export default TopMidBody;